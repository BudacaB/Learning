type Product {
    id: ID!
    name: String!
    price: Float!
}

type Query {
    allProducts: [Product!]!
    product(id: ID!): Product
}

type Mutation {
    createProduct(name: String!, price: Float!): Product!
    deleteProduct(id: ID!): Boolean!
}
